# Base S3 URL for app static assets
app_store_url: 'https://qaihub-public-assets.s3.us-west-2.amazonaws.com'

# Base GitHub URL for the apps repo (no ref, no /apps suffix).
# The ref is injected at import time via --ref (e.g. refs/tags/v1.0.0).
app_repo_base: 'https://github.com/qualcomm/ai-hub-apps'
